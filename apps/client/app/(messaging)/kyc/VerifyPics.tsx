import { trpc } from '@/lib';
import React from 'react';

export default function PictureBvn() {
  // const { isLoading, data: quiz_topics } = trpc.quiz_topics.get_all.useQuery();
  return <div>PictureBvn</div>;
}
